# HSK1 ClassPro 对话交接摘要（2026-08-09）

## 当前项目状态

项目路径：
`D:\F盘\大连东软信息学院\教学\25-26-2\AI\新HSK教程`

当前重点已经从 L07-L10 课程数据制作，推进到两个系统级模块：

1. 看图表达 / Picture Talk + 云墙
2. HSK mock test / 测

L07-L10 已有课程数据，L08/L09/L10 已补看图表达数据：

- L08：6 题，方位词口语句型讲解 + 方位词看图表达。
- L09：12 题，覆盖 4 个方位词问答句型。
- L10：8 题，练习“多少钱？”“贵不贵？”等问价表达。

Mock Test 已有 `HSK1-mock-01` 40 题：

- 听力 20 题。
- 阅读 20 题。
- 教师端控制开放答题、锁定、查看报告。
- 学生端接收 `mock_test`，提交独立记录。
- 当前正式音频仍未验收通过，需要替换现有 TTS 音频。

## 最近已完成的修改

- 教师端首页已形成“讲 / 练 / 赛 / 测”四大板块。
- 看图表达已作为课中练习模块接入。
- 看图表达教师展示页不直接显示参考答案。
- 学生端收到的是 `open_response` 专用题目 payload。
- 云墙支持显示学生姓名、回答、点赞、教师修改和 1-5 分评分。
- 云墙评分会写入当前课堂积分。
- Mock Test 教师端已有全班报告、个人错题、听力/阅读薄弱方向、高错误率题统计。
- 学生端 mock test 不显示音频按钮，听力由教师端统一播放。

## 当前仍需重点处理的问题

### 1. 看图表达 / 云墙视觉和流程复测

待复测点：

- 看图表达进入界面宽度是否真正接近上方答题栏。
- 云墙宽度是否真正加宽。
- 切换到讲解、练习、比赛、测试时，云墙是否自动消失。
- L08/L09/L10 的每一题在答题前是否都能显示图片。
- 大班 20 多人提交后，云墙回答卡是否能合理铺开。
- 教师点开回答、保存评分后，弹窗是否稳定关闭。

### 2. HSK Mock Test 正式音频

当前方向：

- 暂时先试 TTS Maker。
- 按大题生成 4 段音频：
  - 1-5
  - 6-10
  - 11-15
  - 16-20
- 每题朗读两遍。
- 每题之间保留 4-5 秒停顿。
- 语速慢速，普通话清晰。
- 对话题如果 TTS Maker 支持多角色，优先男女声交替；如果不支持，先用单女声版本测试。

已生成 TTS Maker 文本：

- `docs/mock-test-audio/HSK1-mock-01-L1_picture_choice-TTSMaker.txt`
- `docs/mock-test-audio/HSK1-mock-01-L2_answer_choice-TTSMaker.txt`
- `docs/mock-test-audio/HSK1-mock-01-L3_dialogue_picture_choice-TTSMaker.txt`
- `docs/mock-test-audio/HSK1-mock-01-L4_sentence_question_answer-TTSMaker.txt`
- `docs/mock-test-audio/HSK1-mock-01-all-listening-TTSMaker.txt`

后续流程：

1. 教师复制每个大题 TXT 内容到 TTS Maker。
2. 生成 4 个音频文件。
3. 人工听辨普通话、语速、停顿、音量、自然度。
4. 将音频文件放入：
   `source/data-model/mock-tests/audio/`
5. 更新 `source/data-model/mock-tests/HSK1-mock-01.json` 中 `audioParts[].file`。
6. 本地测试教师端播放。

建议命名：

- `HSK1-mock-01-listening-01.mp3`
- `HSK1-mock-01-listening-02.mp3`
- `HSK1-mock-01-listening-03.mp3`
- `HSK1-mock-01-listening-04.mp3`

### 3. Mock Test 后续深化

待完成：

- 答题卡深度优化。
- 锁定恢复线上压力测试。
- 教师报告导出。
- `mock_test` 数据记录与课中互动积分进一步隔离。
- 听力音频正式替换并上线验收。

### 4. 批改台与 GAS

当前状态：

- 批改台前端已改过一轮。
- GAS v2 也有修改。
- 但后续仍需线上复测。

待处理：

- 保存或归档必须以后端真实成功为准。
- 主观题判定正确后，学生端报告应显示正确和得分。
- 正常作答批量通过、自动反馈、归档闭环需要继续验收。
- 明确 GitHub Pages 前端更新和 Google Apps Script 后端部署是两回事。

### 5. 课程数据后续

待处理：

- L10 已人工校对一轮，但新增看图表达、mock test 接入后仍需整体流程测试。
- L09 仍建议继续人工审核课前、语法、课文 Q&A、课后作业质量。
- 后续 L11+ 数据制作时要继续参考数据质量文档，避免只重复课文原句。

## 建议下一轮推进顺序

1. 先复测看图表达宽度、图片显示、云墙消失问题。
2. 用 TTS Maker 生成 4 段 mock test 听力音频。
3. 把新音频接入网页，替换当前浏览器 TTS / 旧 WAV。
4. 本地完整测试 mock test：教师开放、播放音频、学生答题、提交、锁定、报告。
5. 再处理批改台 + GAS 线上复测。
6. 最后回到 L10/L11+ 课程数据。

## 新对话提示词

请继续推进我的 HSK1 ClassPro 项目。

项目路径：
`D:\F盘\大连东软信息学院\教学\25-26-2\AI\新HSK教程`

请先读取并参考：

- `docs/开发问题清单-HSK1-L01-L02.md`
- `docs/对话交接摘要-2026-08-09.md`
- `docs/HSK1-mock-test开发方案.md`
- `source/data-model/mock-tests/HSK1-mock-01.json`
- `source/in-class/teacher.html`
- `source/in-class/student.html`

当前优先级：

1. 复测并修复看图表达 / 云墙：
   - 看图表达进入界面宽度要接近上方答题栏。
   - 云墙宽度也要加宽。
   - L08/L09/L10 每一题答题前都要显示图片。
   - 切换讲解、练习、比赛、测试时，云墙必须消失。
   - 大班学生提交时，云墙回答卡应合理铺开。

2. HSK Mock Test 音频：
   - 我会先用 TTS Maker 试做 4 段听力音频。
   - 已生成文本在 `docs/mock-test-audio/`。
   - 等我把音频文件放到项目后，请帮我接入网页。
   - 学生端不要显示音频按钮，教师端统一播放。
   - 每题听两遍，每题之间 4-5 秒停顿。

3. Mock Test 流程测试：
   - 教师端开放答题。
   - 学生端接收并答题。
   - 教师端锁定。
   - 查看全班报告和个人错题报告。
   - 确认 `mock_test` 记录不和课中互动积分混在一起。

4. 后续再处理批改台和 GAS 线上复测。

工作原则：

- 不要直接发布线上，除非我明确要求。
- 不要大改页面框架，优先小范围修复。
- 所有新课程内容必须放入数据文件，页面只做通用渲染。
- 教师端界面统一中文显示，不直接暴露英文内部字段。
- 修改后必须本地验证教师端、学生端和数据结构。

